import React from 'react';

/**
 * Functional component representing the header of the web page.
 *
 * @returns {JSX.Element} - Rendered header with the title.
 */
function Header() {
    return (
        <div className="header">
            {/* Title of the web page */}
            <h1>Kailee's Magic Mood Ring</h1>
        </div>
    );
}

export default Header;
